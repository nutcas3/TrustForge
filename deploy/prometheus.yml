global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: trustforge
    static_configs:
      - targets: ["api:9090"]
    metrics_path: /metrics

  - job_name: postgres
    static_configs:
      - targets: ["postgres:9187"]
